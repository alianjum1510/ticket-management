{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "DOCKERFILE",
    "dockerfilePath": "Dockerfile"
  },
  "deploy": {
    "startCommand": "/bin/sh -c \"mkdir -p /app/data && alembic upgrade head && python -m scripts.seed_users && python -m scripts.seed_tickets && uvicorn main:app --host 0.0.0.0 --port ${PORT:-8000}\"",
    "healthcheckPath": "/docs",
    "healthcheckTimeout": 100,
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
